#include<stdio.h>
/**
 * main - Entry point to a c program
 *
 * Description: Program to print a string using printf
 * Return: 0 (Success)
 */
int main(void)
{
	char myString[56] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s", myString);
	return (0);
}
